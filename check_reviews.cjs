const fs=require("fs"); const {Client}=require("pg"); async function t(){ let url=fs.readFileSync(".env","utf8").match(/DATABASE_URL="([^"]+)"/)[1].split("?")[0]; const c=new Client({connectionString:url, ssl:{rejectUnauthorized:false}}); await c.connect(); const res=await c.query(`SELECT column_name FROM information_schema.columns WHERE table_name='reviews'`); console.log(res.rows.map(r=>r.column_name).join(", ")); await c.end(); } t();
